import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#2f5d62',
    },
    secondary: {
      main: '#d98e04',
    },
  },
  shape: {
    borderRadius: 8,
  },
});
